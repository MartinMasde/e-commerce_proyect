<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito de Compras</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: white;
            color: black;
        }
        .card {
            background-color: #f5f5dc;
            border: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .card-title {
            text-align: center;
            font-weight: bold;
        }
        .cart-info {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Carrito de Compras</h1>

        {{#if user}}
        <div class="cart-info">
            <h4>Información del Usuario</h4>
            <p class="mb-1"><strong>Nombre:</strong> {{user.firstName}} {{user.lastName}}</p>
            <p class="mb-0"><strong>Email:</strong> {{user.email}}</p>
        </div>
        {{/if}}

        {{#if products.length}}
            <div class="row mt-4">
                {{#each products}}
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">{{this._id.title}}</h5>
                                <p class="card-text"><strong>Categoría:</strong> {{this._id.category}}</p>
                                <p class="card-text"><strong>Descripción:</strong> {{this._id.description}}</p>
                                <p class="card-text"><strong>Precio:</strong> ${{this._id.price}}</p>
                                <p class="card-text"><strong>Cantidad:</strong> {{this.qty}}</p>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        {{else}}
            <div class="alert alert-info text-center mt-4">
                El carrito está vacío
            </div>
        {{/if}}
    </div>
</body>
</html>